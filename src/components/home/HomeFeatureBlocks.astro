---
import { Image } from 'astro:assets'
import { Icon } from 'astro-icon/components'
import Button from '@components/shared/Button.astro'
import checkmark from '@images/illustrations/checkmark.svg'
import portraitImage1 from '@images/stock/hj-portrait.jpg'
import squareImage1 from '@images/stock/ucla-img.jpg'
import portraitImage2 from '@images/stock/hj-portrait.jpg'
import squareImage2 from '@images/stock/ucla-img.jpg'

const features = [
  'Personalized',
  'Small classes',
  'Montessori',
  'STEM focused',
  'Hands-on approach',
  'Diverse teachers & students',
  'Experienced staff',
]

const blocks = [
  {
    tagline: 'WE ARE ELITE',
    headline: "베이글 에듀 대표 소개",
    text: 'USC 석사 재학중',
    text2: 'UCLA 및 서울대 MBA 졸업',
    text3: '미국 대학 입시 학습 트레이닝 전문 강사',
    text4: '서울 외국인학교 담임교사',
    text5: '크레버스 CMS 영재관 강사',
    action: { label: 'Learn more', href: '/about', icon: true },
    portraitImage: { src: portraitImage1, alt: 'Kid stacking blocks' },
    squareImage: { src: squareImage1, alt: 'Children writing on workbook' },
  }
]
---

<section
  class="overflow-hidden bg-yellow-100 px-4 pb-16 sm:px-6 sm:pb-24 lg:px-8 mb-16"
>
  {/* Container */}
  <div class="mx-auto max-w-screen-xl">
    {/* Centered content with feature list */}
    <div class="relative">
      {/* Block title and subtext */}
      <h2 class="h2 mx-auto max-w-4xl text-center text-purple-900">
        영어 학습을 재미있고 자신감 넘치게 이끌어 가며 각 학생의 영어 실력 향상과 대학 진학을 위한 발 딛음.
      </h2>
      <p
        class="mx-auto mt-4 max-w-3xl text-center text-xl leading-relaxed text-purple-800"
      >
        제가 찾은 답은요?
        완벽이 아닌 완성을 목표로 공부를 합니다. 모르면 튜터에게 물어보자! 알게된 사실은 기록 하자! 입니다. 어려운 결정은 즐기자 - 학업 방향을 결정 해야 달라집니다. 학생 학부모 튜터와 함께 성장하자 - 멈춰 있지만 않으면 괜찮습니다.
      </p>
      {/* Feature list */}
      <div class="mx-auto mt-12 max-w-3xl">
        <ul
          class="-mx-3 -my-2 flex flex-wrap items-center justify-center text-lg text-purple-800"
        >
          {
            features.map((feature) => (
              <li class="mx-3 my-2 flex items-center">
                <Image
                  class="mr-3 h-7 w-7 flex-shrink-0"
                  src={checkmark}
                  alt=""
                />
                <span>{feature}</span>
              </li>
            ))
          }
        </ul>
      </div>
    </div>

    {
      blocks.map((block, index) => (
        <div
          class:list={[
            'mx-auto mt-16 max-w-xl lg:grid lg:max-w-none lg:grid-cols-12 lg:gap-x-14 xl:gap-x-20 2xl:gap-x-24',
            index % 2 == 0
              ? 'sm:mt-20 lg:mt-24'
              : 'sm:mt-44 lg:mt-56 xl:mt-60 2xl:mt-64',
          ]}
        >
          {/* Block images*/}
          <div
            class:list={[
              'relative lg:col-span-6',
              { 'lg:order-2': index % 2 == 1 },
            ]}
          >
            <div
              class:list={[
                'relative',
                index % 2 == 0
                  ? 'sm:pl-36 lg:pl-20 xl:pl-32'
                  : 'sm:pr-36 lg:pr-20 xl:pr-32',
              ]}
            >
              <div class="aspect-h-4 aspect-w-3 relative rounded-2xl">
                <Image
                  class="absolute inset-0 rounded-2xl object-cover"
                  src={block.portraitImage.src}
                  alt={block.portraitImage.alt}
                />
              </div>
              <div
                class:list={[
                  'absolute hidden rounded-3xl sm:block sm:h-72 sm:w-72 lg:h-64 lg:w-64 xl:h-72 xl:w-72 2xl:h-80 2xl:w-80',
                  index % 2 == 0
                    ? 'bottom-0 left-0 sm:translate-y-1/3'
                    : 'right-0 top-0 sm:-translate-y-1/3',
                ]}
              >
                <Image
                  class="absolute inset-0 h-full w-full rounded-3xl object-cover"
                  src={block.squareImage.src}
                  alt={block.squareImage.alt}
                />
              </div>
            </div>
          </div>
          {/* Block text content*/}
          <div
            class:list={[
              'mt-16 flex flex-col justify-center lg:col-span-6 lg:mt-0',
              index % 2 == 0 ? 'sm:mt-44' : 'sm:mt-20 lg:order-1',
            ]}
          >
            <div>
              <span class="inline-block -rotate-1 rounded-full bg-purple-200 px-4 py-2 font-medium text-purple-700 shadow-md">
                {block.tagline}
              </span>
            </div>
            <h3 class="h3 mt-4 text-purple-900 sm:mt-5">{block.headline}</h3>
            <p class="mt-3 max-w-2xl text-lg leading-loose text-purple-800">
              {block.text}
            </p>
            <p class="mt-3 max-w-2xl text-lg leading-loose text-purple-800">
              {block.text2}
            </p>
            <p class="mt-3 max-w-2xl text-lg leading-loose text-purple-800">
              {block.text3}
            </p>
            <p class="mt-3 max-w-2xl text-lg leading-loose text-purple-800">
              {block.text4}
            </p>
            <p class="mt-3 max-w-2xl text-lg leading-loose text-purple-800">
              {block.text5}
            </p>
            <div class="mt-6">
              <Button href={block.action.href} variant="accent" size="sm">
                {block.action.label}
                {block.action.icon && (
                  <Icon
                    name="tabler:arrow-narrow-right"
                    class="ml-3 h-6 w-6 group-hover:animate-horizontal-bounce"
                  />
                )}
              </Button>
            </div>
          </div>
        </div>
      ))
    }
  </div>
</section>
