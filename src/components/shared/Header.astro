---
import Navbar from './Navbar.astro'

const pathname = Astro.url.pathname.replace(/(?<=.)\/$/, '')
---

<header class:list={[pathname === '/parents' ? 'bg-white' : 'bg-purple-25']}>
  <Navbar />
</header>
