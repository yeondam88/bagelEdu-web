---
import Layout from '../../layouts/Layout.astro';
import { getCollection } from 'astro:content';
import { Image } from 'astro:assets';

export async function getStaticPaths() {
  const posts = await getCollection('blog');
  return posts.map(post => ({
    params: { slug: post.slug },
    props: { post },
  }));
}

const { post } = Astro.props;
const { Content } = await post.render();
---

<Layout title={`${post.data.title} | Bright`}>
  <main class="container mx-auto px-4 py-12 max-w-4xl">
    <button 
      onclick="history.back()" 
      class="mb-8 flex items-center gap-2 text-gray-600 hover:text-primary transition-colors"
    >
      <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
        <path d="m15 18-6-6 6-6"/>
      </svg>
      Back
    </button>

    <article>
      <Image 
        src={post.data.image} 
        alt={post.data.title} 
        width={1200}
        height={600}
        class="w-full h-[400px] object-cover rounded-xl mb-8"
      />
      <h1 class="text-4xl font-bold mb-4">{post.data.title}</h1>
      <div class="flex gap-4 text-gray-500 mb-8">
        <time>{post.data.date}</time>
        <span>·</span>
        <span>{post.data.author}</span>
      </div>
      <div class="prose prose-lg max-w-none">
        <Content />
      </div>
    </article>
  </main>
</Layout>